rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isOwner() {
      return request.auth != null
        && request.auth.uid == "bXTElQJfQ6dm5YvLFuJ1pEGr2Zy2";
    }

    match /{document=**} {
      allow read, write: if isOwner();
    }
  }
}
